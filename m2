 i m adding ner file 
 im practising the git one more time 
 i m doing well 
 im doing git 
 first we clone the repository from the remote repository to the workspace and we modify the repository with our requirment and using some  git commands its push into remote repository
 first we clone the repository from the remote repository to the workspace 
 add some files and modify repository with our requirment and add the modification fille add to the staging .after staging the file move from local repository by using git commit -m command. after alla status completed usrd git pull command and rpositery intoremote repository
add another line in server side 
what is the git merge 
ans git merge is whwtever files are in the main it should be transfer in another branch means 
files  can be one merge to another branch 
